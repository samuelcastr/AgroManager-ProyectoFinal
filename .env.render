# Usar este archivo como referencia para variables en Render
# NO commitear secretos en el archivo .env

# ========================================
# DJANGO SETTINGS
# ========================================
DJANGO_SETTINGS_MODULE=config.settings.prod
DEBUG=False
SECRET_KEY=CAMBIAR_EN_RENDER_CON_CLAVE_SEGURA_AQUI_AQUI_AQUI

# ========================================
# DATABASE (Railway)
# ========================================
DATABASE_URL=mysql://root:HyYShkillcrQSeemhSAkPpgKtxPCbCfa@tramway.proxy.rlwy.net:56935/railway

# ========================================
# HOSTS & SECURITY
# ========================================
ALLOWED_HOSTS=api.tudominio.render.com,www.api.tudominio.render.com,localhost
CSRF_TRUSTED_ORIGINS=https://api.tudominio.render.com,https://www.api.tudominio.render.com
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# ========================================
# CORS
# ========================================
CORS_ALLOWED_ORIGINS=https://api.tudominio.render.com,https://frontend.tudominio.com

# ========================================
# JWT
# ========================================
JWT_ACCESS_LIFETIME=60
JWT_REFRESH_LIFETIME=1

# ========================================
# EMAIL (OPCIONAL - para notificaciones)
# ========================================
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=noreply@tudominio.com
EMAIL_HOST_PASSWORD=tu-app-password

# ========================================
# SENTRY (OPCIONAL - para monitoreo de errores)
# ========================================
SENTRY_DSN=

# ========================================
# STATIC FILES
# ========================================
STATIC_ROOT=/var/lib/render/staticfiles
STATIC_URL=/static/

# ========================================
# LOGGING
# ========================================
LOG_LEVEL=INFO
LOG_FILE_PATH=/var/lib/render/logs/django.log
